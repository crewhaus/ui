# Secrets & env for the `onchain-game` harness.
# Copy this file to `.env` in THIS folder (gitignored); the host loads it for the harness.

# Provider credential — which one you need depends on `agent.model` in your spec.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# OPENAI_API_KEY=sk-xxxxxxxx
# GEMINI_API_KEY=xxxxxxxx

# ── On-chain (EVM) — match the ${VAR} refs in your spec's chain/wallet config ─
# EVM_RPC_URL=https://...
# WALLET_PRIVATE_KEY=0x...      # prefer a kms:// reference in the spec for real funds
